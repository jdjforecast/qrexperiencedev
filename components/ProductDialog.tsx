"use client"

import { Button } from "@/components/ui/button"
;(
  <Button onClick={handleAction} aria-label={`Añadir ${product.name} al carrito`}>
    Añadir al carrito
  </Button>
) < DialogPrimitive.Close
className = "absolute right-4 top-4 rounded-sm opacity-70"
aria-label = "Cerrar diálogo" > <X className="h-4 w-4" />
</DialogPrimitive.Close>

